# 📖 pikGPS 完全新手教學

> **本教學適合誰？**
> 完全沒有程式經驗的使用者。從零開始，一步一步帶你安裝好所有工具，並學會操作每一個功能。只要照著做，你一定可以成功！

---

## 目錄

1. [這個工具是什麼？能做什麼？](#1-這個工具是什麼能做什麼)
2. [你需要準備什麼](#2-你需要準備什麼)
3. [安裝 Node.js（讓程式能跑起來）](#3-安裝-nodejs讓程式能跑起來)
4. [安裝 ADB（電腦與手機的橋樑）](#4-安裝-adb電腦與手機的橋樑)
5. [iOS 裝置設定（iOS 17+）](#5-ios-裝置設定ios-17)
6. [設定 Android 手機](#6-設定-android-手機)
7. [安裝 Appium Settings App](#7-安裝-appium-settings-app)
8. [下載並啟動 pikGPS 工具](#8-下載並啟動-pikgps-工具)
9. [第一次打開網頁：認識畫面](#9-第一次打開網頁認識畫面)
10. [功能教學：設定假 GPS 位置](#10-功能教學設定假-gps-位置)
11. [功能教學：規劃路徑並播放移動（含 GPX 載入）](#11-功能教學規劃路徑並播放移動)
12. [功能教學：最愛地點與最愛路徑](#12-功能教學最愛地點與最愛路徑)
13. [功能教學：GPS 鎖定（Keepalive）](#13-功能教學gps-鎖定keepalive)
14. [功能教學：歷史記錄](#14-功能教學歷史記錄)
15. [（選用）使用 Google Maps](#15-選用使用-google-maps)
16. [常見問題與解決方法](#16-常見問題與解決方法)
17. [名詞解說](#17-名詞解說)

---

## 1. 這個工具是什麼？能做什麼？

### 簡單說
這個工具可以讓你**欺騙 Android 手機，讓手機以為它在另一個地方**。

### 實際用途
- 📍 讓手機 GPS 顯示在你指定的任何地點（例如：東京、紐約、你家附近的公園）
- 🚶 模擬在地圖上行走（讓手機以為你正在移動）
- 🎮 用於需要 GPS 的遊戲或 App 的測試

### 這個工具的工作原理（不用全懂，了解大概就好）

```
你的電腦（網頁控制台）
        ↓  透過 USB 連線
你的 Android 手機
        ↓  修改假的 GPS 座標
手機上的 App 以為你在那個地方
```

你在電腦的網頁上選擇一個地點，程式會透過 USB 線把假的座標發給手機，手機上的 App 就會以為你在那裡。

---

## 2. 你需要準備什麼

### 硬體
| 項目 | 說明 |
|------|------|
| 電腦 | macOS、Windows 或 Linux 皆可 |
| Android 手機 | Android 8.0 以上（如果你的手機是 2018 年以後購買的，通常都沒問題）|
| USB 傳輸線 | 要能**傳輸資料**的線（不是只能充電的那種）|

> **怎麼知道 USB 線能不能傳資料？**
> 插上手機後，手機螢幕上方通知欄如果出現「**USB 連接**」或「**檔案傳輸**」等選項，就是可以傳資料的線。如果只有「**充電中**」，就需要換條線。

### 軟體（稍後會一步一步教你安裝）
- **Node.js** — 讓電腦能執行這個工具的程式
- **ADB** — Android Debug Bridge，讓電腦能跟手機「說話」的工具
- **本工具的程式碼** — 就是你正在看的這個 android-fakegps 專案

---

## 3. 安裝 Node.js（讓程式能跑起來）

Node.js 是一種讓電腦可以執行 JavaScript 程式的工具。你不需要了解它是什麼，只需要安裝它。

### macOS 使用者

**方法一：直接下載安裝（推薦新手）**

1. 打開瀏覽器，前往 [https://nodejs.org](https://nodejs.org)
2. 你會看到兩個綠色按鈕，選左邊標著「**LTS**」的（LTS 代表長期支援版，比較穩定）
3. 下載後，點擊那個 `.pkg` 安裝檔
4. 一直點「繼續」→「同意」→「安裝」，輸入電腦密碼完成安裝

**確認安裝成功：**
1. 按 `Command（⌘） + Space`，輸入「**終端機**」，按 Enter 開啟
2. 在黑色視窗裡輸入以下文字（完整複製貼上），按 Enter：
   ```
   node --version
   ```
3. 如果出現類似 `v20.11.0` 的字樣，代表安裝成功！

---

### Windows 使用者

1. 打開瀏覽器，前往 [https://nodejs.org](https://nodejs.org)
2. 選左邊標著「**LTS**」的版本，下載 Windows Installer（`.msi` 檔案）
3. 點擊下載的安裝檔，一直點「Next」→「I accept the agreement」→「Next」直到安裝完成

> ⚠️ **重要**：安裝過程中，有一頁會問「**Automatically install the necessary tools**」，**請勾選這個選項**！

**確認安裝成功：**
1. 按 `Windows 鍵 + R`，輸入「**cmd**」，按 Enter
2. 在黑色視窗裡輸入以下文字，按 Enter：
   ```
   node --version
   ```
3. 如果出現類似 `v20.11.0` 的字樣，代表安裝成功！

---

### Linux 使用者

打開終端機，輸入：
```bash
sudo apt update && sudo apt install nodejs npm -y
```
輸入密碼後等待安裝完成，再輸入 `node --version` 確認。

---

## 4. 安裝 ADB（電腦與手機的橋樑）

ADB（Android Debug Bridge）是 Google 提供的工具，讓電腦可以跟 Android 手機溝通。

### macOS 使用者（推薦方式）

macOS 上最簡單的安裝方式是使用 **Homebrew**（一個 macOS 的軟體管理工具）。

**步驟一：安裝 Homebrew（如果你還沒有的話）**

1. 打開「終端機」（按 `⌘ + Space` 搜尋「終端機」）
2. 貼上以下整段指令，按 Enter：
   ```
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
   ```
3. 按照提示輸入電腦密碼，等待安裝完成（可能需要幾分鐘）

**步驟二：安裝 ADB**

在終端機輸入：
```
brew install android-platform-tools
```
等待安裝完成。

**確認安裝成功：**

輸入：
```
adb version
```
如果出現類似 `Android Debug Bridge version 1.0.41` 的字樣，代表成功！

---

### Windows 使用者

**步驟一：下載 ADB**

1. 前往 [https://developer.android.com/studio/releases/platform-tools](https://developer.android.com/studio/releases/platform-tools)
2. 找到「**Download SDK Platform-Tools for Windows**」，點擊下載
3. 下載後解壓縮（右鍵 → 解壓縮全部），記住解壓縮的位置（例如：`C:\platform-tools`）

**步驟二：將 ADB 加入 PATH（讓系統找得到 ADB）**

這個步驟是告訴電腦 ADB 在哪裡。

1. 在桌面上對「**本機**」點右鍵 → 選「**內容**」
2. 點擊「**進階系統設定**」
3. 點「**環境變數**」
4. 在「**系統變數**」區域，找到並點選「**Path**」，點「**編輯**」
5. 點「**新增**」，輸入你剛才解壓縮的路徑（例如：`C:\platform-tools`）
6. 一路點「確定」關閉所有視窗

**步驟三：確認安裝成功**

1. 按 `Windows 鍵 + R`，輸入 `cmd`，按 Enter
2. 輸入：
   ```
   adb version
   ```
3. 出現版本資訊代表成功！

---

### Linux 使用者

```bash
sudo apt install adb -y
```

---

## 5. iOS 裝置設定（iOS 17+）

> **只有 iPhone 使用者才需要看這章**，Android 手機使用者可以直接跳到下一章。

### 為什麼 iPhone 需要額外設定？

iOS 的保護機制比 Android 嚴格，假 GPS 需要透過蘋果官方開發工具的授權管道才能注入，不能像 Android 那樣直接用 ADB 送座標。我們用的工具叫 **pymobiledevice3**，它是蘋果官方開發流程的 Python 版本實作。

### 前置條件

| 項目 | 需求 |
|------|------|
| 電腦作業系統 | **macOS**（推薦）或 **Windows 10/11**（進階，需額外步驟）|
| iPhone | iOS 17 或以上 |
| Xcode（macOS 限定）| 15 或以上，從 Mac App Store 安裝（約 7–14 GB）|

> **Windows 使用者注意**：iOS 17+ 的 tunnel 在 Windows 上需要安裝 WinTun 驅動程式，穩定性比 Mac 差，步驟也較繁瑣。建議有條件的話優先使用 Mac。

---

### 步驟一：開啟 iPhone 開發者模式

1. 打開 iPhone 的「設定」
2. 往下滑，找到「隱私權與安全性」
3. 滑到最底部，找到「開發者模式」
4. 開啟開關，手機會要求重新啟動
5. 重啟後會再次詢問是否確認開啟，點「開啟」

> ⚠️ 開啟後手機右上角不會有任何標示，這是正常的。

---

### 步驟二：安裝 pymobiledevice3

**macOS：** 打開 Terminal（終端機），執行：

```bash
# 先安裝 Python 3.13（iOS 18.2+ 必須）
brew install python@3.13

# 進入 fakeGPS 專案目錄，建立虛擬環境並安裝
python3.13 -m venv .venv
.venv/bin/pip install "pymobiledevice3>=9.6"
```

> **為什麼要這樣做？** 因為 macOS 不允許直接安裝 Python 套件（為避免影響系統環境），所以需要建立虛擬環境（.venv）來隔離安裝。這個步驟只需要做一次，之後每次使用都不需要重新建立。
>
> **為什麼需要 Python 3.13？** iOS 18.2 以上已移除 QUIC 協定，必須使用 Python 3.13 才能以 TCP 模式連線。

**Windows：** 打開命令提示字元或 PowerShell，執行：

```powershell
pip install "pymobiledevice3>=9.6"
```

> 若出現 `pip: command not found`，請先至 [https://www.python.org](https://www.python.org) 下載安裝 Python 3，安裝時勾選「**Add Python to PATH**」。

安裝完成後，驗證是否成功：

```bash
.venv/bin/pymobiledevice3 --version
# 看到版本號碼（例如 9.x.x）代表成功
```

---

### 步驟三：連接 iPhone 並信任電腦

1. 用 USB 連接 iPhone 到電腦
2. iPhone 螢幕會跳出「是否信任此電腦？」，點「信任」
3. 輸入 iPhone 密碼確認

> 💡 **WiFi 連線（首次配對後可用）**：完成 USB 信任後，之後的使用不一定需要插線。
> 只要 iPhone 與 Mac 在同一個 WiFi 網路，tunneld 就能自動透過 WiFi 找到裝置。

---

### 步驟四：啟動伺服器（選擇 iOS 模式）

在終端機執行：

```bash
npm start
```

啟動後會出現選單：

```
=== FakeGPS 啟動器 ===

  1) Android 裝置
  2) iOS 裝置（USB / WiFi）

     ⚠️  iOS WiFi 連線前請確認：
        · iPhone 已先用 USB 與 Mac 配對過
        · iPhone 與 Mac 在同一個 WiFi 網路
        · 設定 → 一般 → VPN 與裝置管理 → 開發者模式 已開啟

請選擇要連結的裝置類型 (1/2):
```

輸入 **2** 後按 Enter。啟動器會自動以 sudo 啟動 tunnel（macOS 需輸入登入密碼），tunnel 就緒後伺服器會自動開啟，不需要另開視窗。

> ⚠️ **Windows 使用者**：需先安裝 **WireGuard for Windows**（含 WinTun 驅動，至 [wireguard.com/install](https://www.wireguard.com/install/) 下載），並以**系統管理員身份**開啟 PowerShell 再執行 `npm start`。

---

### 步驟五：確認伺服器已就緒

看到以下訊息即代表成功：

```
通道已啟動，正在啟動伺服器...
伺服器已啟動於 http://localhost:3000
```

開啟瀏覽器前往 [http://localhost:3000](http://localhost:3000) 即可開始使用。

---

## 6. 設定 Android 手機

這個步驟是在手機上開啟「開發者模式」，這是允許電腦跟手機溝通的必要設定。

### 步驟一：啟用開發者選項

> **為什麼需要這個？** 開發者選項預設是隱藏的，需要特殊方式才能解鎖。這是 Android 的設計，防止一般使用者誤觸到技術設定。

1. 打開手機的**設定**（齒輪圖示）
2. 進入「**關於手機**」（有些手機在「系統」→「關於手機」）
3. 找到「**版本號碼**」（有時候叫「**組建編號**」）
4. **連續快速點擊「版本號碼」7 次**
5. 系統可能會要求你輸入 PIN 碼或密碼
6. 當你看到「**您已是開發人員！**」或「**開發者選項已開啟**」的提示，就成功了！

> **不同品牌的手機，設定選單的位置可能不同：**
> - **Samsung**：設定 → 關於手機 → 軟體資訊 → 版本號碼
> - **小米/MIUI**：設定 → 我的裝置 → MIUI 版本
> - **OPPO/vivo**：設定 → 關於手機 → 版本號碼
> - **Google Pixel**：設定 → 關於手機 → 版本號碼

---

### 步驟二：開啟 USB 偵錯

USB 偵錯允許電腦透過 USB 傳送指令給手機。

1. 回到設定主畫面，找到並點入「**開發者選項**」
2. 先確認最上方的開關是**開啟**（藍色）的狀態
3. 向下捲動，找到「**USB 偵錯**」
4. 將它切換為**開啟**
5. 出現警告視窗，點「**確定**」

---

### 步驟三：用 USB 線連接手機到電腦

1. 用 USB 線連接手機和電腦
2. 手機螢幕上會出現「**是否允許 USB 偵錯？**」的提示
3. **勾選「一律允許從這台電腦偵錯」**（這樣以後就不用每次都確認）
4. 點「**允許**」

**確認連接成功：**

在終端機（或命令提示字元）輸入：
```
adb devices
```

如果看到類似下面的輸出，代表連接成功：
```
List of devices attached
ABC123XYZ456	device
```

如果只看到：
```
List of devices attached
```
（後面什麼都沒有），表示手機還沒有連上，請重新確認 USB 偵錯是否已開啟，並且已點擊手機上的「允許」。

---

## 7. 安裝 Appium Settings App

Appium Settings 是一個特殊的 App，它負責在手機端接收假 GPS 座標並套用到系統。

> **為什麼需要這個？** Android 系統規定，只有被指定為「模擬位置應用程式」的 App，才能修改 GPS 座標。Appium Settings 就是這個 App。

### 安裝步驟

Appium Settings 的安裝檔（`appium-settings.apk`）已經包含在這個專案裡，不需要另外下載。

> **想要最新版本？** 本 APK 來自開源專案 [Appium Settings](https://github.com/appium/io.appium.settings)，如需自行取得最新版，可前往 [Releases 頁面](https://github.com/appium/io.appium.settings/releases)下載。

1. 確認 USB 偵錯已開啟，手機透過 USB 連接到電腦
2. 打開終端機，`cd` 進入專案資料夾（下一節會教你怎麼做，或直接繼續看步驟 3）
3. 在終端機輸入：
   ```
   adb install appium-settings.apk
   ```
4. 等待幾秒，看到「**Success**」代表安裝成功

> **如果你現在還不知道怎麼 `cd` 進入專案資料夾，沒關係！** 先把步驟 7 做完、下載好專案之後，再回來執行這個指令。

### 授予 Appium Settings 位置權限

> ⚠️ **請不要直接打開 Appium Settings App！**
> Appium Settings 的 App 介面一打開就會閃退，這是**正常現象**，不是你做錯了。它本來就不是給人手動操作的 App，而是在背景默默接收假 GPS 座標用的。

安裝完後，你需要手動從手機「設定」裡授予它位置權限：

1. 打開手機的**設定**
2. 進入「**應用程式**」（有些手機叫「App 管理」）
3. 找到並點擊「**Appium Settings**」
4. 點選「**權限**」
5. 點選「**位置**」
6. 選擇「**一律允許**」

> 💡 如果看不到「一律允許」選項，選「允許」或「使用 App 時允許」也可以，不過「一律允許」效果最好。

---

### 設定 Appium Settings 為模擬位置 App

安裝好之後，還需要告訴手機「請用 Appium Settings 來處理假 GPS」。

1. 打開手機的**設定**
2. 進入「**開發者選項**」
3. 找到「**選取模擬位置應用程式**」（有些手機叫「**模擬位置 App**」）
4. 從清單中選擇「**Appium Settings**」

---

## 8. 下載並啟動 pikGPS 工具

### 下載專案

**如果你有安裝 Git：**

打開終端機，輸入：
```bash
git clone https://github.com/YOUR_USERNAME/android-fakegps.git
```

**如果你沒有安裝 Git（更簡單的方式）：**

1. 前往 GitHub 上的專案頁面
2. 點擊綠色的「**Code**」按鈕
3. 選擇「**Download ZIP**」
4. 下載後解壓縮到你想放的位置（例如：桌面）

---

### 進入專案資料夾

**macOS / Linux：**

打開終端機，輸入：
```bash
cd ~/Desktop/android-fakegps
```
（如果你放在其他地方，請把 `~/Desktop/android-fakegps` 換成你實際的路徑）

**Windows：**

打開命令提示字元，輸入：
```
cd C:\Users\你的使用者名稱\Desktop\android-fakegps
```

> **小技巧**：你也可以先打 `cd ` (注意後面有空格)，然後把資料夾從檔案總管**拖進**終端機視窗，路徑會自動填入！

---

### 安裝程式所需的套件

第一次使用時，需要安裝這個工具所依賴的其他程式。在終端機輸入：
```
npm install
```

等待安裝完成（可能需要一兩分鐘，你會看到很多文字跑過去，這是正常的）。

---

### 啟動伺服器

> **📱 iOS 使用者必看（Android 可跳過）**
>
> 執行 `npm start` 後選擇 **2（iOS）**，啟動器會自動幫你建立 tunnel，不需要另開終端機。
> 請先確認 iPhone 已透過 USB 連接且已信任電腦（詳見第 5 章步驟三）。

```
npm start
```

如果成功，你會看到類似：
```
🚀 Server running at http://localhost:3000
```

**不要關閉這個終端機視窗！** 它需要一直開著，工具才能運作。

---

### 打開網頁控制台

打開你的瀏覽器（Chrome 或 Firefox），在網址列輸入：
```
http://localhost:3000
```

按 Enter，你應該會看到一個有地圖的網頁！

---

## 9. 第一次打開網頁：認識畫面

![畫面示意](示意圖)

網頁分成兩個區域：

```
┌─────────────────────────────────────────────────────────┐
│  左側控制台（側邊欄）              │  右側地圖             │
│                                    │                       │
│  ┌──────────────────────────────┐  │  [地圖，可點擊]        │
│  │ Google Maps API Key 輸入框  │  │                       │
│  ├──────────────────────────────┤  │                       │
│  │ 裝置選擇（下拉選單）        │  │                       │
│  ├──────────────────────────────┤  │                       │
│  │ 位置設定                    │  │                       │
│  │  - 搜尋框                   │  │                       │
│  │  - 座標輸入                 │  │                       │
│  │  - ✓ 改變定位  ↩           │  │                       │
│  │  - 最愛地點                 │  │                       │
│  │  - 歷史記錄                 │  │                       │
│  ├──────────────────────────────┤  │                       │
│  │ 路徑規劃                    │  │                       │
│  └──────────────────────────────┘  │                       │
└─────────────────────────────────────────────────────────┘
```

### 各區塊說明

| 區塊 | 功能 |
|------|------|
| **Google Maps API Key** | 輸入 Google Maps 的金鑰（可以不填，使用免費地圖）|
| **裝置選擇** | 下拉選單，選擇要發送假 GPS 的裝置（支援多台 Android / iOS 同時連線）|
| **位置設定** | 輸入或搜尋你要假裝在的地點 |
| **路徑規劃** | 設定一條路線並模擬沿著路線移動 |

---

### 確認裝置已連線

在左側側邊欄，找到「**裝置選擇**」下拉選單。

**Android 使用者：**
- 若下拉選單出現裝置型號（例如 `Pixel 7 (Android)`）→ 連線成功，系統會自動選取！
- 若顯示「**未偵測到裝置**」→ 請回到[步驟 6](#6-設定-android-手機)確認設定

**iOS 使用者：**
- 程式啟動後會自動每 3 秒偵測一次（最多 60 秒），期間會顯示「tunneld 掃描中…」。偵測成功後下拉選單會出現 iPhone 名稱（例如：`iPhone 15 Pro (iOS) 🔌 USB`），下方會顯示 DVT 連線狀態。
- 若下拉選單出現「⚠ iOS 連線中斷（Channel is closed）」的紅色提示 → 請重新啟動伺服器（Ctrl+C → `npm start`）
- 如果超過 60 秒仍顯示「**未偵測到裝置**」→ 確認 `tunneld` 已在執行，且手機已信任電腦

**多台裝置：** 同時連接多台裝置時，下拉選單會列出所有裝置，從中選取即可切換目標。

點「**重新整理**」按鈕可以重新偵測裝置。

---

## 10. 功能教學：設定假 GPS 位置

### 方法一：搜尋地點名稱

1. 在左側「**位置設定**」區塊，點擊「**地點搜尋**」輸入框
2. 輸入地點名稱（中文或英文都可以），例如：「台北 101」或「Tokyo Tower」
3. 按 **Enter**
4. 地圖會跳到那個位置，並出現一個**黃色的圓形標記**（這是預覽標記，還沒真正送出）
5. 確認位置是你要的地方後，點擊「**✓ 改變定位**」按鈕
6. 左側出現「✓ 已送出」的提示，地圖上的**紅色標記**移到新位置
7. 手機的 GPS 現在應該顯示在那個位置了！

> **什麼是預覽標記？**
> 黃色標記表示「你選了這個位置，但還沒有真正發送給手機」。這讓你可以先確認位置是否正確，再決定要不要送出。按「✓ 改變定位」才會真正改變手機的 GPS。

---

### 方法二：直接點擊地圖

1. 在右側地圖上，點擊你想要的位置
2. 地圖上出現**黃色預覽標記**，座標輸入框也會自動填入
3. 確認後點「**✓ 改變定位**」

---

### 方法三：手動輸入座標

1. 在「**座標（緯度, 經度）**」輸入框中輸入座標
2. 格式：`緯度, 經度`（用逗號分隔），例如：`25.047800, 121.531900`
3. 按 **Enter** 顯示預覽標記
4. 點「**✓ 改變定位**」送出

> **怎麼取得座標？**
> - **Google Maps**：在網頁版 Google Maps 上，對著你想要的地點點右鍵，第一行就是座標，點一下就會複製
> - **手機 Google Maps**：按住任意位置，上方會出現座標

---

### ↩ 按鈕（回到確認位置）

如果你在選位置的過程中，把地圖拖到很遠的地方，找不到紅色標記了，按一下「**↩**」圓形按鈕，地圖視角就會自動跳回目前確認的 GPS 位置（紅色標記的地方）。

---

## 11. 功能教學：規劃路徑並播放移動

這個功能讓你模擬沿著一條路線移動，就像在地圖上行走一樣。

你可以**手動在地圖上點擊設定航點**，也可以**載入 GPX 檔案**自動填入路線。

### 方法 A：手動在地圖上設定航點

### 步驟一：開啟航點模式

點擊「**開啟航點模式**」按鈕，按鈕會變色，表示現在進入了設置路線的模式。

### 步驟二：在地圖上設定目的地

在地圖上點擊你想要走到的地方，這些點叫做「**航點**」。

- 每點一次，地圖上會出現一個帶有數字的紫色標記（1、2、3…）
- 紫色標記之間會有藍色的線連起來，代表你的路線
- **只需要設定 1 個航點**，路線會從你目前確認的 GPS 位置（紅色標記）自動出發
- 如果你要走比較複雜的路線，可以設定多個航點

### 步驟三：調整移動速度

「**速度**」滑桿可以設定移動速度，範圍是 1 到 50 km/h。

- **1–5 km/h**：模擬正常步行
- **10 km/h**：模擬慢跑
- **30–50 km/h**：模擬騎腳踏車或開車

### 步驟四：開始播放

點「**▶ 開始播放**」，你會看到：
- 地圖上出現藍色路線
- 紅色標記開始沿著路線移動
- 座標輸入框的數字會即時更新

### 步驟五：停止播放

點「**■ 停止**」按鈕，移動立即停止，GPS 鎖定在停止時的位置。

### 注意事項

> ⚠️ **距離警告**：如果你設的第一個航點跟目前位置相差超過 20 公里，播放前會彈出確認視窗問你「確定要這樣走嗎？」因為這樣的跳躍可能看起來不自然。

---

### 方法 B：載入 GPX 檔案

如果你有 GPX 格式的路徑檔案（例如從登山 App、健身記錄或地圖網站匯出的路線），可以直接載入，不需要手動一個一個點擊航點。

#### 什麼是 GPX 檔？

GPX（GPS Exchange Format）是一種常見的 GPS 路線格式，副檔名為 `.gpx`。許多 App 都可以匯出，例如 Strava、Garmin Connect、Google My Maps 等。

#### 如何載入 GPX

1. 點擊「**📂 載入 GPX**」按鈕
2. 從電腦選取一個 `.gpx` 檔案
3. 載入完成後：
   - 檔案裡的所有座標點會自動填入為航點（地圖上顯示紫色標記）
   - **起點（第一個座標）會立即自動送出**，手機 GPS 會跳到路線起點
   - 底部狀態列會顯示「✓ 已載入 N 個航點，起點已送出」

4. 確認路線後，調整速度，按「**▶ 開始播放**」

> 💡 **關於點數限制**：GPX 檔案有時包含數千個座標點。為了避免地圖卡頓，系統會自動將超過 300 點的路線均勻取樣至 300 點，起點與終點都會保留。取樣時狀態列會顯示「原始 N 點已取樣至 300 點」。

> 💡 **重複載入**：可以多次點擊「📂 載入 GPX」選取不同的 GPX 檔，每次載入都會清除舊的航點並重新填入。

---

## 12. 功能教學：最愛地點與最愛路徑

這個功能讓你儲存常用的地點和路線，下次可以一鍵載入，不用每次重新設定。

### 儲存最愛地點

1. 先設定好你想儲存的位置（透過搜尋或點地圖，並按「✓ 改變定位」確認）
2. 點「**★ 加入最愛地點**」按鈕
3. 輸入這個地點的名稱（例如：「公司」「家」「台北 101」）
4. 點「確定」，地點就儲存了

### 載入最愛地點

- 點清單中地點旁邊的「**📍**」按鈕
- 地圖會跳到那個位置，並顯示黃色預覽標記
- 確認後按「**✓ 改變定位**」送出

### 刪除最愛地點

- 點地點旁邊的「**✕**」按鈕

---

### 儲存最愛路徑

1. 先設定好你的航點（至少 1 個）
2. 點「**★ 儲存路徑**」按鈕
3. 輸入路線名稱（例如：「上班路線」「公園散步」）
4. 點「確定」儲存

### 載入最愛路徑

- 點路線旁邊的「**▶**」按鈕，航點會自動恢復
- 再按「**▶ 開始播放**」就可以出發了

---

## 13. 功能教學：GPS 鎖定（Keepalive）

### 為什麼需要這個功能？

Android 系統有時候會把假 GPS 還原回真實位置（尤其是一段時間沒有收到更新時）。GPS 鎖定功能會讓這個工具**每隔 2 秒自動重新傳送一次座標**，確保手機的 GPS 一直維持在你設定的位置。

### 怎麼使用

- **送出座標後**，左側側邊欄的「ADB 裝置」區塊會出現「**🔒 GPS 鎖定中**」
- 這表示鎖定已經自動開啟，你不需要做任何事情

### 暫停鎖定

如果你想讓手機 GPS 恢復正常：
1. 點「**暫停鎖定**」按鈕
2. 狀態變為「**⏸ 未鎖定**」，幾秒後手機 GPS 會回到真實位置

### 恢復鎖定

點「**恢復鎖定**」，重新開始傳送假 GPS。

---

## 14. 功能教學：歷史記錄

每次你按「✓ 改變定位」確認位置時，這個位置就會自動記錄在歷史清單裡（最多保留 10 筆）。

### 查看歷史記錄

在「位置設定」區塊下方，找到「**歷史記錄（N）**」，點擊它展開清單。

### 載入歷史位置

點清單中任一筆旁邊的「**📍**」按鈕，地圖會跳到那個位置並顯示黃色預覽標記，確認後按「**✓ 改變定位**」送出。

---

## 15. （選用）使用 Google Maps

這個工具預設使用免費的 OpenStreetMap（不需要任何帳號或金鑰）。如果你有 Google Maps API Key，可以切換到 Google Maps，支援更精準的地點搜尋。

> 如果你不知道什麼是 API Key，或不想花時間申請，**跳過這一節即可**，OpenStreetMap 已經很夠用了。

### 取得 Google Maps API Key

1. 前往 [https://console.cloud.google.com](https://console.cloud.google.com)（需要 Google 帳號）
2. 建立一個新專案（點左上角選單 → 「選取專案」→「新增專案」）
3. 搜尋並啟用「**Maps JavaScript API**」和「**Places API**」
4. 前往「**API 和服務**」→「**憑證**」→「**建立憑證**」→「**API 金鑰**」
5. 複製你的 API 金鑰

> ⚠️ **注意費用**：Google Maps API 有免費額度，一般個人使用不太會超過。但建議你在 Cloud Console 設定「每月預算上限」保險。

### 輸入 API Key

1. 在網頁控制台最上方找到「**Google Maps API Key**」輸入框
2. 貼上你的 API Key
3. 點「**套用並重新載入**」
4. 頁面重新載入後，地圖就會切換成 Google Maps

### 切回 OpenStreetMap

點「**清除（用免費地圖）**」，頁面重新載入後切回 OpenStreetMap。

---

## 16. 常見問題與解決方法

### ❌ 問題：打開網頁後地圖不顯示

**解決方法：**
- 確認終端機還在執行（`npm start` 的視窗還開著）
- 確認瀏覽器網址是 `http://localhost:3000`（不是 `https`，是 `http`）
- 如果使用 Google Maps 金鑰，確認金鑰正確且相關 API 已啟用

---

### ❌ 問題：ADB 裝置顯示「未偵測到裝置」

請依序確認：

1. **USB 線是否插好？** 試著拔掉重插
2. **USB 線是否支援傳輸資料？** 換一條線試試
3. **手機 USB 偵錯是否開啟？** 設定 → 開發者選項 → USB 偵錯
4. **手機有沒有出現「允許 USB 偵錯」的彈窗？** 回到手機看一下，點「允許」
5. **電腦有沒有安裝手機驅動程式？**（Windows 使用者特別要注意）前往手機品牌的官方網站下載 USB 驅動程式

在終端機輸入 `adb devices` 確認：
```
List of devices attached
R5CW20XXXXX	device   ← 有這行代表連接成功
```

---

### ❌ 問題：GPS 設定後手機的 App 沒有反應

1. 確認「**Appium Settings**」已安裝在手機上（手機 App 清單裡找得到）
2. 確認已在「開發者選項」→「選取模擬位置應用程式」中，選擇了「**Appium Settings**」
3. 某些手機（例如 MIUI）需要在 Appium Settings 的 App 設定中，允許「**在背景執行**」的權限

---

### ❌ 問題：GPS 維持假位置一段時間後自動恢復真實位置

確認側邊欄是否顯示「**🔒 GPS 鎖定中**」。如果沒有：
- 重新按一次「**✓ 改變定位**」，觸發鎖定機制
- 或點「**恢復鎖定**」按鈕

---

### ❌ 問題：在 Windows 上輸入 `adb` 或 `node` 出現「不是有效的命令」

這代表這些程式的路徑還沒有加入 PATH。

**ADB 的解決方法**：確認有按照[第 4 章的 Windows 部分](#4-安裝-adb電腦與手機的橋樑)，將 platform-tools 資料夾加入 PATH。加入後**重新開啟**命令提示字元再試。

**Node.js 的解決方法**：重新執行 Node.js 安裝程式，確保勾選了「Add to PATH」選項，然後重啟電腦。

---

### ❌ 問題：`npm install` 出現很多錯誤

最常見的原因是網路問題。試試看：
```
npm install --registry https://registry.npmjs.org
```

如果你在中國大陸，可以使用鏡像：
```
npm install --registry https://registry.npmmirror.com
```

---

### ❌ 問題：安裝 appium-settings.apk 失敗

**錯誤訊息：`INSTALL_FAILED_ALREADY_EXISTS`**
→ Appium Settings 已經安裝過了！不需要重新安裝。

**錯誤訊息：`INSTALL_FAILED_VERSION_DOWNGRADE`**
→ 手機上有更新版本的 Appium Settings，不需要重新安裝。

**錯誤訊息：`error: no devices/emulators found`**
→ 手機沒有正確連接，請重新確認 USB 偵錯設定。

---

### ❌ 問題（iOS）：裝置狀態顯示「未偵測到裝置」

請依序確認：

1. **tunneld 有沒有在執行？** 選 iOS 模式啟動時，確認終端機有顯示 `[tunneld]` log 訊息
2. **iPhone 有沒有跳出「信任此電腦」？** 回到 iPhone 螢幕確認，若沒有跳出，拔掉 USB 重新插
3. **開發者模式是否已開啟？** 設定 → 隱私權與安全性 → 開發者模式（需要開啟並重啟手機一次）
4. **pymobiledevice3 是否安裝正確？** 執行 `.venv/bin/pymobiledevice3 usbmux list` 看有無輸出

---

### ❌ 問題（iOS）：GPS 設定後 iPhone 的 App 沒有反應

請確認：

1. **iOS 版本是否為 17 以上？** 低於 iOS 17 不支援
2. **tunneld 是否持續執行中？** 若已停止，重新執行 `npm start` 並選 iOS 模式
3. **某些 App 可能有自己的防偵測機制**，這超出本工具的範圍

---

### ❌ 問題（iOS）：執行 tunneld 時出現錯誤

常見錯誤與解決方式：

| 錯誤訊息 | 解決方式 |
|----------|----------|
| `command not found: pymobiledevice3` | 執行 `python3.13 -m venv .venv && .venv/bin/pip install "pymobiledevice3>=9.6"` |
| `xcode-select: error...` | 執行 `xcode-select --install` 安裝 Xcode Command Line Tools |
| `Permission denied` | 確認前面加了 `sudo`，並輸入 Mac 登入密碼 |
| iPhone 無法偵測 | 拔除 USB，重啟 tunneld，再重新插上 USB |

---

### ❌ 問題（iOS）：tunneld 出現 `QuicProtocolNotSupportedError`

你的 iPhone 是 iOS 18.2 以上，已移除 QUIC 協定，需要 Python 3.13 才能使用 TCP 模式。請執行：

```bash
brew install python@3.13
python3.13 -m venv .venv
.venv/bin/pip install "pymobiledevice3>=9.6"
```

---

### ❌ 問題（iOS）：裝置選擇顯示「⚠ iOS 連線中斷（Channel is closed）」

DVT 通道因各種原因（手機鎖屏、WiFi 切換、系統逾時等）意外關閉。

**解決方法：** 重新啟動伺服器（按 Ctrl+C 停止，再執行 `npm start`）。前端會自動重新偵測。

---

### ❌ 問題（iOS）：等待超過 60 秒後顯示「逾時，請確認裝置後手動重試」

**解決方法：**

1. 確認 iPhone 與 Mac 在同一個 WiFi 網路
2. 也可以改用 USB 連線（插線後重新點「重新整理」）
3. tunneld 初次掃描到 WiFi 裝置可能需要 15–30 秒，這是正常現象

---

### ❌ 問題（Android）：MIUI（小米手機）設定 GPS 後還是不動

MIUI 有額外的安全限制。請：
1. 在手機設定中找到「**Appium Settings**」的 App 資訊
2. 進入「**電池**」設定，選擇「**無限制**」
3. 進入「**權限**」，確認所有位置相關權限都已開啟
4. 在 MIUI 的「開發者選項」中，找到並關閉「**MIUI 優化**」（關閉後重新開機）

---

## 17. 名詞解說

本教學中用到的一些專業名詞，如果你不懂，可以在這裡查看簡單的說明。

| 名詞 | 解釋 |
|------|------|
| **Node.js** | 一種讓電腦可以執行 JavaScript 程式的工具，這個工具的後端就是用它來運作的 |
| **npm** | Node.js 的套件管理工具，`npm install` 就是在安裝這個工具需要的各種程式庫 |
| **ADB** | Android Debug Bridge，Google 提供的工具，讓電腦可以跟 Android 手機溝通、傳送指令 |
| **終端機 / 命令提示字元** | 黑色的文字輸入視窗，你可以在裡面輸入指令讓電腦做事 |
| **localhost** | 代表「你自己的電腦」。`http://localhost:3000` 就是「開啟在你電腦 3000 號連接埠上跑的網頁」|
| **GPS 座標** | 用一組數字（緯度和經度）來表示地球上任何一個地點的位置 |
| **緯度（Latitude）** | 南北方向的座標，台灣大約在 22–25 度 |
| **經度（Longitude）** | 東西方向的座標，台灣大約在 120–122 度 |
| **預覽標記** | 黃色圓形標記，代表「候選位置但還沒送出」|
| **確認位置 / 紅色標記** | 紅色標記，代表已經真正送給手機的 GPS 位置 |
| **Keepalive（GPS 鎖定）** | 每隔 2 秒重新傳送一次座標，確保手機 GPS 不會自動還原 |
| **航點** | 規劃路徑時設定的中途點，紫色數字標記 |
| **GPX** | GPS Exchange Format，一種常見的 GPS 路線格式（副檔名 `.gpx`），可從 Strava、Garmin、Google My Maps 等 App 匯出 |
| **APK** | Android Application Package，Android App 的安裝檔（相當於 Windows 的 `.exe` 或 macOS 的 `.dmg`）|
| **API Key** | 使用某個服務（例如 Google Maps）的「鑰匙」，證明你有權使用這個服務 |
| **OpenStreetMap** | 一個免費的開放地圖，不需要 API Key，本工具預設使用 |
| **Nominatim** | OpenStreetMap 提供的地點搜尋服務 |
| **localStorage** | 瀏覽器本地儲存空間，用來儲存你的設定，即使重新整理頁面也不會消失 |
| **USB 偵錯** | Android 開發者選項中的設定，開啟後電腦才能透過 USB 傳送指令給手機 |
| **PATH** | 一個系統設定，告訴電腦「如果找不到這個指令，要去哪些地方找」|

---

## 完成！🎉

恭喜你！如果你照著這份教學做完了，你應該已經：

**Android 使用者：**
- ✅ 安裝好 Node.js
- ✅ 安裝好 ADB
- ✅ 設定好 Android 手機的開發者選項與 USB 偵錯
- ✅ 安裝好 Appium Settings 並授予位置權限
- ✅ 成功啟動 Fake GPS 控制台
- ✅ 學會使用基本功能

**iOS 使用者：**
- ✅ 安裝好 Node.js
- ✅ 安裝好 Xcode 與 pymobiledevice3
- ✅ 開啟 iPhone 開發者模式
- ✅ 執行 `npm start` 選 2 時，tunneld 會自動啟動（sudo），支援 USB 與 WiFi 連線
- ✅ 成功啟動 Fake GPS 控制台
- ✅ 學會使用基本功能

如果過程中遇到任何問題，先查看[常見問題](#16-常見問題與解決方法)，或到專案的 GitHub Issues 頁面回報問題。

---

> **免責聲明**：本工具僅供個人開發測試使用。在遊戲或應用程式中使用假 GPS 可能違反服務條款，請自行評估風險並負擔相應責任。
